export interface Request {
    fullName: string, //firebase Id
    saiSamithi: string,
    state: string,
    city: string,
    pinCode?: number,
    phoneNumber: number,
    address: string,
    orgName?: string,
    quantity: number
}
